"use client";import {useState} from "react";
export default function LessonClient({lesson,progress}:{lesson:any,progress:any}){const[done,setDone]=useState(Boolean(progress?.completed));const[video,setVideo]=useState(lesson.youtubeVideoId||"");async function complete(){const r=await fetch("/api/progress",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({lessonId:lesson.id,completed:true,secondsWatched:0})});if(r.ok)setDone(true)}return <div><span className="pill">{lesson.code} · {lesson.chapter.part.title}</span><h1>{lesson.title}</h1><div className="grid grid2"><div className="card"><div className="video">{video?<iframe src={`https://www.youtube-nocookie.com/embed/${video}?rel=0`} title={lesson.title} allowFullScreen/>:<div style={{color:"white",textAlign:"center",padding:50}}>الفيديو لم يُضف بعد من لوحة الإدارة.</div>}</div><h2>أهداف الدرس</h2><ul>{(lesson.objectives||[]).map((x:string)=><li key={x}>{x}</li>)}</ul><button className="btn" onClick={complete}>{done?"✓ تم إكمال الدرس":"تحديد الدرس كمكتمل"}</button></div><div className="card"><h2>تمارين الكتاب</h2><pre style={{whiteSpace:"pre-wrap",lineHeight:1.8,fontFamily:"inherit",maxHeight:600,overflow:"auto"}}>{lesson.sourceExerciseText||"لا توجد تمارين مضافة لهذا الدرس."}</pre></div></div></div>}
