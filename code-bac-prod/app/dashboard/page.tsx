import {getCurrentUser} from "@/lib/auth";import {db} from "@/lib/db";import {redirect} from "next/navigation";
export default async function Dashboard(){const u=await getCurrentUser();if(!u)redirect("/login");if(u.role==="ADMIN")return <div><h1>أهلاً {u.name}</h1><p className="muted">لوحة إدارة CODE BAC جاهزة.</p><a className="btn" href="/dashboard/admin">فتح لوحة الإدارة</a></div>;const s=u.student;if(!s)return <div className="card"><h1>أهلاً {u.name}</h1><p>الحساب لا يحتوي على ملف طالب بعد.</p></div>;const total=await db.lesson.count();const done=await db.lessonProgress.count({where:{studentId:s.id,completed:true}});const subs=await db.codingSubmission.count({where:{studentId:s.id}});const paid=(await db.enrollment.findUnique({where:{studentId_programId:{studentId:s.id,programId:"code-bac-program"}}}))?.paid??false;const pct=total?Math.round(done/total*100):0;return <main><div style={{display:"flex",justifyContent:"space-between",alignItems:"center"}}><div><h1>أهلاً {u.name} 👋</h1><p className="muted">حسابك الحقيقي متصل بقاعدة البيانات.</p></div><span className="pill">{paid?"مشترك":"غير مشترك"}</span></div><div className="grid grid3" style={{marginTop:18}}><div className="card"><div className="muted">التقدم</div><div className="stat">{pct}%</div></div><div className="card"><div className="muted">الدروس المكتملة</div><div className="stat">{done}/{total}</div></div><div className="card"><div className="muted">محاولات Coding</div><div className="stat">{subs}</div></div></div><div className="card" style={{marginTop:18}}><h2>كورس البرمجة والذكاء الاصطناعي</h2><div className="progress"><i style={{width:`${pct}%`}}/></div><p className="muted">{pct}% مكتمل</p>{paid?<a className="btn" href="/dashboard/courses">استكمل التعلم</a>:<a className="btn" href="/dashboard/checkout">اشترك الآن</a>}</div></main>}
