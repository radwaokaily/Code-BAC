"use client";import {useState} from "react";import {useRouter} from "next/navigation";
export default function Login(){const r=useRouter();const[email,setEmail]=useState("");const[p,setP]=useState("");const[e,setE]=useState("");const[reg,setReg]=useState(false);const[name,setName]=useState("");async function submit(){const url=reg?"/api/auth/register":"/api/auth/login";const body=reg?{name,email,password:p}:{email,password:p};const x=await fetch(url,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const d=await x.json();if(!x.ok)return setE(d.error||"حدث خطأ");if(reg){setReg(false);setE("تم إنشاء الحساب. سجل الدخول الآن.");return}r.push(d.mustChangePassword?"/change-password":"/dashboard");r.refresh()}return <div style={{maxWidth:520,margin:"70px auto"}}><div className="card"><span className="pill">CODE BAC</span><h1>{reg?"إنشاء حساب طالب":"تسجيل الدخول"}</h1>{reg&&<input className="input" placeholder="الاسم" value={name} onChange={e=>setName(e.target.value)}/>}<input className="input" style={{marginTop:10}} placeholder="البريد الإلكتروني" value={email} onChange={e=>setEmail(e.target.value)}/><input className="input" style={{marginTop:10}} type="password" placeholder="كلمة المرور" value={p} onChange={e=>setP(e.target.value)}/><button className="btn" style={{marginTop:12}} onClick={submit}>{reg?"إنشاء الحساب":"دخول"}</button><button className="btn secondary" style={{marginTop:8}} onClick={()=>{setReg(!reg);setE("")}}>{reg?"لدي حساب بالفعل":"إنشاء حساب جديد"}</button>{e&&<p className="notice">{e}</p>}</div></div>}
