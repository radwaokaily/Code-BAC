"use client";import {useState} from "react";import {useRouter} from "next/navigation";
export default function ChangePassword(){const r=useRouter();const [a,setA]=useState("");const[b,setB]=useState("");const[c,setC]=useState("");async function save(){if(b!==c)return setC("كلمتا المرور غير متطابقتين");const x=await fetch("/api/auth/password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({currentPassword:a,newPassword:b})});const d=await x.json();if(!x.ok)return setC(d.error||"حدث خطأ");r.push("/dashboard");r.refresh()}return <div style={{maxWidth:520,margin:"70px auto"}}><div className="card"><span className="pill">أمان الحساب</span><h1>غيّر كلمة المرور</h1><p className="muted">هذه الخطوة مطلوبة لأول دخول للحساب الإداري.</p><input className="input" type="password" placeholder="كلمة المرور الحالية" value={a} onChange={e=>setA(e.target.value)}/><input className="input" type="password" placeholder="كلمة المرور الجديدة" value={b} onChange={e=>setB(e.target.value)} style={{marginTop:10}}/><input className="input" type="password" placeholder="تأكيد كلمة المرور الجديدة" value={c} onChange={e=>setC(e.target.value)} style={{marginTop:10}}/><button className="btn" onClick={save} style={{marginTop:12}}>حفظ</button>{c&&!c.includes("متطابقتين")&&<p className="notice">{c}</p>}</div></div>}
